辞書に関する主な命令

len(d)
  dの項目数を返す
d.keys()
  dに含まれるキーのリストを返す
d.values()
  dに含まれる値のリストを返す
k in d
  キーｋがdの中にあればTrueを返す
d[k]
  dの中でキーkをもつ値を返す
d.get(k, v)
  kがdの中にあればd[k]を返し、なければvを返す
d[k] = v
  dに値ｖをキーｋで登録する
  すでにkで登録された値があれば、その値は置き換えられる
del d[k]
  dからキーkを削除する
for k in d
  dのすべてのキーによって反復する
